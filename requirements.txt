# Python工作流引擎依赖包

# YAML配置文件解析
PyYAML>=6.0

# HTTP请求支持
aiohttp>=3.8.0  # 异步HTTP请求（HttpNode需要）
requests>=2.28.0  # 同步HTTP请求（备用）

# 定时任务支持
croniter>=2.0.0  # Cron表达式解析（TimerNode需要）

# 数据库支持
aiomysql>=0.2.0  # 异步MySQL客户端（MysqlNode需要）

# 可选：更好的YAML支持
ruamel.yaml>=0.17.0

# 模板引擎支持
Jinja2>=3.1.0

# 开发依赖（可选）
# pytest>=7.0.0  # 用于单元测试
# black>=22.0.0  # 代码格式化
# flake8>=4.0.0  # 代码检查
